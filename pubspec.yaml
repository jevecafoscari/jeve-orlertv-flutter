name: jeve_orlertv_flutter
description: Progetto per l'app Orler TV.


publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 0.2.2+13


environment:
  sdk: ">=2.12.0 <3.0.0"


dependencies:
  flutter:
    sdk: flutter

  # Media
  video_player: ^2.4.6

  # Utilità
  visibility_detector: ^0.3.3
  url_launcher: ^6.1.5
  path_provider: ^2.0.11
  wordpress_api:
    path: ../dart-wp
  html: ^0.15.0
  flutter_html: ^2.2.1
  wakelock: ^0.6.2

  # Grafica
  cupertino_icons: ^1.0.5
  new_gradient_app_bar: ^0.2.0
  # google_fonts: ^2.1.0
  swatcher: ^0.1.0
  cached_network_image: ^3.2.1


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.1
  build_runner: ^2.2.0
  # flutter packages pub run build_runner build
  r_flutter: ^0.9.0

  # flutter pub run flutter_launcher_icons:main
  flutter_launcher_icons: ^0.10.0


flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/
    - assets/fonts/montserrat/
    - assets/images/favaro/
    - assets/images/mestre/
    - assets/images/campiglio/
    - assets/images/abano/
    - assets/images/products/jewels/
    - assets/images/products/carpets/
    - assets/images/products/paintings/


flutter_icons:
  ios: true
  android: true
  image_path_ios: "assets/icons/ic_launcher_ios.jpg"
  image_path_android: "assets/icons/ic_launcher_android.png"
